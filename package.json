{
  "name": "aeon-main",
  "private": true,
  "scripts": {
    "ci": "npm run test:version && npm run version:check && npm --prefix implementations/typescript run ci",
    "ci:typescript": "npm --prefix implementations/typescript run ci:typescript",
    "ci:canonical": "npm --prefix implementations/typescript run ci:canonical",
    "ci:rust": "cargo fmt --all --check --manifest-path implementations/rust/Cargo.toml && cargo check --workspace --all-targets --manifest-path implementations/rust/Cargo.toml && cargo clippy --workspace --all-targets --manifest-path implementations/rust/Cargo.toml && cargo test --workspace --all-targets --all-features --manifest-path implementations/rust/Cargo.toml",
    "ci:rust:verbose": "cargo fmt --all --check --manifest-path implementations/rust/Cargo.toml && cargo check --workspace --all-targets --manifest-path implementations/rust/Cargo.toml && cargo clippy --workspace --all-targets --manifest-path implementations/rust/Cargo.toml && cargo test --workspace --all-targets --all-features --manifest-path implementations/rust/Cargo.toml -- --nocapture",
    "ci:full": "npm run ci && npm run ci:rust",
    "tests:all": "npm run ci:full && python3 ./scripts/stress-combinations.py --run both --brief --failures-only",
    "test:canonical:corpus": "python3 ./scripts/stress-canonical-corpus.py",
    "test:canonical:corpus:php": "python3 ./scripts/stress-canonical-corpus.py --php",
    "test:sofia:deep-stack": "cargo test --release --manifest-path implementations/rust/Cargo.toml -p aeon-core sofia_deep_stack_stress -- --ignored --test-threads=1 --nocapture",
    "test:sofia:differential": "python3 ./scripts/check-sofia-parser-differential.py",
    "test:sofia:wasm:cts": "npm --prefix implementations/typescript run test:cts:wasm:core",
    "test:sofia:wasm:browser:serve": "pnpm --dir implementations/typescript --filter @altopelago/aeon-wasm build && pnpm --dir implementations/typescript --filter @altopelago/aeon-wasm test:browser:serve",
    "validate:cts-claims": "node ./scripts/validate-cts-claims.mjs",
    "version:check": "node ./scripts/version.mjs check",
    "version:check-tag": "node ./scripts/version.mjs check-tag",
    "version:set": "node ./scripts/version.mjs set",
    "version:recover": "node ./scripts/version.mjs recover",
    "test:version": "node --test ./test/version-tool.test.mjs",
    "oracle:gen": "node stress-tests/tools/gen-oracle.mjs",
    "oracle:gen:dry-run": "node stress-tests/tools/gen-oracle.mjs --dry-run"
  }
}
