# Test Fixtures

These files are local, non-authoritative snapshots used to keep AEON package
tests reproducible in standalone checkouts.

`altopelago.aeonic-limits.v1.aeon` mirrors the canonical policy from
`AltoPelago/aes` at commit `8816c403e698f2a6fd1dd47b08cfc44b099f4740`.
The AES repository remains authoritative; update this snapshot deliberately
when the pinned AES revision changes.
